DOMAIN = "chauffeur_conversation"
CONF_BASE_URL = "base_url"
# Auth arc S7: the credential Chauffeur now expects. Optional, because a
# household mid-migration still has the local-origin grace switched on and
# forcing a token here would take Argyle away before they had one to paste.
CONF_SERVICE_TOKEN = "service_token"
DEFAULT_BASE_URL = "http://local-chauffeur:8000"
